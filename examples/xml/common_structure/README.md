# Common structure examples

This directory contains examples demonstrating how to read and write common structure elements in AUTOSAR XML files using the `autosar` Python library.

Currently there is only one example related to reading and writing `AdminData` elements. The example scripts are:

- `read_admin_data.py`
- `write_admin_data.py`
